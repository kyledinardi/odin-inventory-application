extends filmDetails

block delete 
  h2 Are you sure you want to delete this film?
  p This action cannot be undone.
  form(method="post") 
    input(type="hidden", name="filmId", value=film._id)
    button(type="submit") Delete #{film.title}